# 唯一任务：生成一个回译题

只使用下方 `<verified_read_source>` 中的英文，并严格遵循 `<selection_length>` 给出的当前目标长度和上限。可以是一句或两个连续短句。

要求：

- `sourceEn` 必须逐字复制原文中的一段连续文本，不得改写、拼接不连续句子或补充内容。
- `promptZh` 给出自然、准确的中文，不显示任何英文答案。
- 专有名词在中文后用括号附上原英文。
- `focus` 只写一个主要训练重点，例如“被动语态与时间关系”或“原因—结果结构”。
- 不向用户解释选题过程。

最终只输出下面的结构化标记；程序会校验并转换成用户可见题目：

<!--RTO_BT_SELECTION
{"sourceEn":"逐字复制的英文原句","promptZh":"自然中文题目","focus":"一个训练重点"}
RTO_BT_SELECTION-->
